const express= require('express');

const classesRouter=express.Router();

classesRouter.get("/", (res,req)=>{})
classesRouter.get("/", (res,req)=>{})
classesRouter.get("/", (res,req)=>{})
classesRouter.get("/", (res,req)=>{})

classesRouter.get("/:id/student",()=>{
    const classDoc=db.classes.findOne()
    
})

module.exports = classesRouter;